name: tp0
services:
  server:
    container_name: server
    image: server:latest
    entrypoint: python3 server/main.py
    environment:
      - LOGGING_LEVEL=DEBUG
    networks:
      - testing_net
    volumes:
      - ./server:/server
